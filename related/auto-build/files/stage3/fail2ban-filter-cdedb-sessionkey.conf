# Fail2Ban configuration file
#
# Author: CdE Datenbankteam
[Definition]
# Matches messages such as:
# CdEDB invalid session key from 10.0.2.2
journalmatch = _SYSTEMD_UNIT=cdedb-app.service LOGGER=cdedb.backend.session
failregex = CdEDB invalid session key from <HOST>
ignoreregex =
