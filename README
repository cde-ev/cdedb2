== CdEDB v2 ==

The documentation can be accessed at https://db.cde-ev.de/doc/ online. The
source can be found under doc/source/*.rst and to compile it to html pages
issue the command 'make doc' (needs sphinx). The link manual.html redirects
to the documentation (which is available after running 'make doc') for
convenience.
