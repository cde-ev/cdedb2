Liebes Akademieteam,

{{ orga|persona_name(include_nickname=true) }} wurde der Veranstaltung "{{ event.title }}"
durch {{ user|persona_name(include_nickname=true) }} als Orga hinzugefügt.

Viele Grüße

Die Datenbank
