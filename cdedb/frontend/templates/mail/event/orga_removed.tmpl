Liebes Akademieteam,

{{ orga|persona_name(include_nickname=true) }} wurde von der Veranstaltung "{{ event.title }}"
durch {{ user|persona_name(include_nickname=true) }} als Orga entfernt.

Viele Grüße

Die Datenbank
