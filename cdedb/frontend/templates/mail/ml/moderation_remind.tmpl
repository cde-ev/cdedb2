Hallo {{ persona.given_names }},

Für die von dir moderierten Mailinglisten in der CdE-Datenbank sind
aktuell insgesamt {{ total }} Nachrichten in der Moderation.

Es folgt eine Zusammenfassung der zu moderierenden Nachrichten:

{% for ml, held_messages in messages.items() %}
Liste: {{ ml.title }}
Link: {{ cdedblink('ml/message_moderation_form', {'mailinglist_id': ml['id']}) }}

{% set cutoff = 3 %}
{% for message in held_messages[:cutoff] %}
- Absender: {{ message.sender }}
  Betreff: {{ message.subject }}
  Datum: {{ message.hold_date|datetime(lang="de") }}
  Grund für das Zurückhalten: {{ message.reason }}
  Spam-Score: {{ spam_scores[message.request_id] }}
{% endfor %}
{% if held_messages|length > cutoff %}
... und {{ held_messages|length - cutoff }} weitere.
{% endif %}

{% endfor %}

Die Datenbank
