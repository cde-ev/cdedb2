<<% extends "tex/de/event/base_article.tmpl" %>>
<<% block extra_preamble %>>
    \KOMAoptions{fontsize=10pt,paper=landscape,DIV=21}
    \usepackage[left=.5cm,right=.5cm,top=1.5cm,bottom=1cm,foot=1ex]{geometry}
    \parindent 0pt\relax

    \usepackage{lastpage}
    \usepackage{fancyhdr}
    \pagestyle{fancy}

    \renewcommand{\headrulewidth}{0pt}
    \chead{\Large\textbf{\textsf{Teilnehmerliste <<< ambience['event']['title']|te >>>}}}
    \cfoot{Seite \thepage{} von \pageref{LastPage}}

    \usepackage{longtable}
    \usepackage{xcolor}
    \usepackage{colortbl}
    \usepackage{tabu}
    \usepackage{booktabs}
<<% endblock %>>
<<% block content %>>
    \taburowcolors[2] 2{white .. black!5}
    \begin{longtabu}{ll>{\raggedright}p{.3\linewidth}l<<< ('l'*(ambience['event']['parts']|length))|te >>>l}
    \toprule
    Vorname & Nachname & Ort &
    <<% for part_id, pdata in ambience['event']['parts']|dictsort -%>>
        <<< pdata['title']|te >>> &
    <<% endfor -%>>
    Email \\
    \midrule
    \endfirsthead
    \midrule
    Vorname & Nachname & Ort & Handy &
    <<% for part_id, pdata in ambience['event']['parts']|dictsort -%>>
        <<< pdata['title']|te >>> &
    <<% endfor -%>>
    Email \\
    \midrule
    \endhead
    \midrule
    \endfoot
    \bottomrule
    \endlastfoot
    <<% for registration_id in ordered -%>>
        <<% with rdata = registrations[registration_id] -%>>
            <<< personas[rdata['persona_id']]['given_names']|te >>> &
            <<< personas[rdata['persona_id']]['family_name']|te >>> &
            <<< personas[rdata['persona_id']]['postal_code']|te >>>
            <<< personas[rdata['persona_id']]['location']|te >>>
            <<% if personas[rdata['persona_id']]['country'] != "Deutschland" -%>>
                <<< personas[rdata['persona_id']]['country']|te >>>
            <<%- endif -%>>
            &
            <<< personas[rdata['persona_id']]['mobile']|te >>> &
            <<% for part_id in ambience['event']['parts']|sort -%>>
                <<% if rdata['parts'][part_id]['status'] == enums['RegistrationPartStati'].participant -%>>
                    <<% if rdata['parts'][part_id]['course_id'] -%>>
                        <<% if rdata['parts'][part_id]['course_instructor'] == rdata['parts'][part_id]['course_id'] -%>>
                            \emph{%
                                <<< courses[rdata['parts'][part_id]['course_id']]['shortname']|te >>>
                            }
                        <<% else -%>>
                            <<< courses[rdata['parts'][part_id]['course_id']]['shortname']|te >>>
                        <<% endif -%>>
                    <<% elif rdata['persona_id'] in ambience['event']['orgas'] -%>>
                        \emph{Orga}
                    <<% else -%>>
                        % nichts
                    <<% endif -%>>
                <<% else -%>>
                    ---
                <<% endif -%>>
                &
            <<% endfor -%>>
            <<< personas[rdata['persona_id']]['username']|te >>> \\
        <<% endwith -%>>
    <<% endfor -%>>
    \end{longtabu}
<<% endblock %>>
