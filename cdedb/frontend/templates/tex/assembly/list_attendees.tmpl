<<# This is just a part of a tex document, so we do not inherit the base template. -#>>

% All attendees:
<<% for attendee in attendees.all.values() -%>>
\participant{<<< attendee|persona_name()|te >>>}
<<% endfor %>>

% Early attendees (cutoff: <<< attendees.cutoff|datetime >>>):
<<% for attendee in attendees.early.values() -%>>
\participant{<<< attendee|persona_name()|te >>>}
<<% endfor %>>

% Late attendees (cutoff: <<< attendees.cutoff|datetime >>>):
<<% for attendee in attendees.late.values() -%>>
\participant{<<< attendee|persona_name()|te >>>}
<<% endfor %>>

% Other attendees:
<<% for attendee in attendees.undetermined.values() -%>>
\participant{<<< attendee|persona_name()|te >>>}
<<% endfor %>>
