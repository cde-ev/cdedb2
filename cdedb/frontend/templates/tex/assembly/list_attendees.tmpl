\COMMENT{ This is just a part of a tex document, so we do not inherit the base template. }

\BLOCK{- for id, attendee in attendees.items() -}
\participant{\VAR{ attendee['given_names'] }}{\VAR{ attendee['family_name'] }}
\BLOCK{ endfor }
