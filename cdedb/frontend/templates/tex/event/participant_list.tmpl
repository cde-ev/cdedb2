<<% extends "tex/event/base_article.tmpl" %>>
<<% block extra_preamble %>>
    \KOMAoptions{fontsize=10pt,paper=<<< orientation|te >>>,DIV=21}
    \usepackage[left=.5cm,right=.5cm,top=1.5cm,bottom=1cm,foot=1ex]{geometry}
        \parindent 0pt\relax

    \usepackage{longtable}
    \usepackage[table]{xcolor}
    \usepackage{colortbl}
    \usepackage{amssymb}
    \usepackage{tabu}
    \usepackage{booktabs}
    \usepackage{lastpage}
    \usepackage{fancyhdr}
    \pagestyle{fancy}

    \renewcommand{\headrulewidth}{0pt}
    \newcommand{\head}[1]{\textcolor{white}{\textbf{#1}}}

    \definecolor{design}{HTML}{000000}

    \chead{\Large\textbf{\textsf{Teilnehmerliste <<< ambience['event']['title']|te >>>}}}
    \cfoot{Seite \thepage{} von \pageref{LastPage}}

<<% endblock %>>
<<% block content %>>
	\rowcolors{2}{gray!25}{white}
    \begin{longtable}{
    |llll
    <<%- if ambience['event']['parts']|length > 1 or ambience['event']['tracks']|length > 0 -%>>
        <<%- for ep_id, ep in parts.items() -%>>
            <<%- if ep['tracks']|length > 1 -%>>
                <<<- ('l'*ep['tracks']|length) ->>>
            <<%- else -%>>
                <<<- 'l' ->>>
            <<%- endif -%>>
        <<%- endfor -%>>
    <<%- endif -%>>
    |}
            \hline
            \rowcolor{design}
			  \head{Vorname}
			& \head{Nachname}
			& \head{E-Mail}
			& \head{PLZ \& Ort}
            % Kursschienen/Eventparts
            <<% for ep_id, ep in parts|dictsort('part_begin') if ambience['event']['parts']|length > 1 or ambience['event']['tracks']|length > 0 -%>>
			    <<% if ep['tracks']|length < 1 -%>>
                    & \head{<<< ep.shortname|te >>>}
                <<% endif -%>>
                <<% for t_id, t in ep['tracks']|dictsort('sortkey') -%>>
				    & \head{<<< t.shortname|te >>>}
			    <<% endfor -%>>
            <<% endfor -%>>
            \\
			\hline
		\endhead
    <<% set participant = enums['RegistrationPartStati'].participant -%>>
    <<% for registration_id in ordered if orgas_only or registrations[registration_id]['list_consent'] -%>>
        <<% with registration = registrations[registration_id] %>>
            % Vorname
            <<< personas[registration['persona_id']]['given_names']|te >>> &
            % Nachname
            <<< personas[registration['persona_id']]['family_name']|te >>> &
            % E-Mail
            <<< personas[registration['persona_id']]['username']|te >>> &
            % PLZ, Ort (Land)
            <<< personas[registration['persona_id']]['postal_code']|te >>>
            <<< personas[registration['persona_id']]['location']|te >>>
            <<% if personas[registration['persona_id']]['country'] != "Deutschland" -%>>
                <<<- personas[registration['persona_id']]['country']|te ->>>
            <<% endif -%>>
            % Kurse
            <<% for ep_id, ep in parts|dictsort('part_begin') if ambience['event']['parts']|length > 1 or ambience['event']['tracks']|length > 0 -%>>
                <<% if ep['tracks']|length < 1 -%>>
                    <<% if registration['parts'][ep_id]['status'] == participant -%>>
                        & \checkmark
                    <<%- else -%>>
                        & ---
                    <<%- endif -%>>
                    % <<< ep['shortname']|te >>>
                <<% endif -%>>
                <<% for t_id, t in ep['tracks']|dictsort('sortkey') -%>>
                    & % <<< t['shortname']|te >>>
                    <<% if registration['parts'][t['part_id']]['status'] == enums['RegistrationPartStati'].participant -%>>
                        <<% if registration['tracks'][t_id]['course_id'] -%>>
                            <<% if registration['tracks'][t_id]['course_instructor']
                                    == registration['tracks'][t_id]['course_id'] -%>>
                                \emph{
                                    <<<- courses[registration['tracks'][t_id]['course_id']]['shortname']|te ->>>
                                }
                            <<%- else -%>>
                                <<<- courses[registration['tracks'][t_id]['course_id']]['shortname']|te ->>>
                            <<% endif -%>>
                        <<% elif registration['persona_id'] in ambience['event']['orgas'] -%>>
                            \emph{Orga}
                        <<% else -%>>
                            \checkmark
                        <<% endif -%>>
                    <<% else -%>>
                        ---
                    <<% endif -%>>
                <<% endfor -%>>
            <<% endfor -%>>
            \\
        <<% endwith -%>>
    <<% endfor -%>>
        \hline
    \end{longtable}
<<% endblock %>>
