{% extends "web/base.tmpl" %}
{% block title %}
    {% trans -%}
    	Error
    {%- endtrans %}
{% endblock %}
{% block content %}
    {% if kind == "database" %}
        <p>
            {% trans -%}
            	A database transaction went wrong. Please try again.
            {%- endtrans %}
        </p>
    {% elif kind == "quota" %}
        <p>
            {% trans -%}
            	You reached the internal Limit for databse requests.
                This is a security feature to prevent cloning of the database.
                Unfortunately this can lead to false positives.
                The Counter will reset within a few days.
            {%- endtrans %}
        </p>
    {% else %}
        <p>
            {% trans -%}
                An error occurred. This probably means that there is a Bug in the Code, which You stumbled upon.
            {%- endtrans %}
        </p>

        {% if message %}
            <p>
                {% trans message=message|e -%}
                	The Error Message was <strong>{{ message }}</strong>.
                {%- endtrans %}
            </p>
        {% endif %}
    {% endif %}
    
    <p>
        {% trans team=gettext("CdE-Datenbank development team"),
                 mail="<a href='mailto:" + gettext("CdE-Datenbank development team") +
                      "<cdedb@lists.cde-ev.de>'>cdedb@lists.cde-ev.de</a>",
                 now=now()|datetime(lang=lang)|e -%}
        	Should the Problem persist, please contact the {{ team }} at {{ mail }}.
            Be sure to include a description of how to reproduce this error, as well as the current time ({{ now }}).
        {%- endtrans %}
    </p>
{% endblock %}
