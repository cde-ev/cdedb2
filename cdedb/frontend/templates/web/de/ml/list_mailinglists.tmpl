{% set sidenav_active='ml_all' %}
{% extends "web/de/ml/base.tmpl" %}
{% import "web/de/util.tmpl" as util with context %}
{% block title %}Mailinglisten Komplettübersicht{% endblock %}
{% macro format_entry(mailinglist_id, title) %}
    <li>
        {{ util.deko_checkbox(mailinglist_id in subscriptions) }}
        {{ util.href(cdedblink("ml/show_mailinglist", {'mailinglist_id': mailinglist_id}), title) }}
        {% if not all_mailinglists[mailinglist_id]['is_active'] %}
            (inaktiv)
        {% endif %}
    </li>
{% endmacro %}
{% block content %}
    <h2>Allgemeine Listen</h2>
    <ul>
        {% for mailinglist_id, title in mailinglists|dictsort %}
            {% if  (not all_mailinglists[mailinglist_id]['event_id']
                    and not all_mailinglists[mailinglist_id]['assembly_id']) %}
                {{ format_entry(mailinglist_id, title) }}
            {% endif %}
        {% endfor %}
    </ul>
    <h2>Veranstaltungslisten</h2>
    <ul>
        {% for mailinglist_id, title in mailinglists|dictsort %}
            {% if  all_mailinglists[mailinglist_id]['event_id'] %}
                {{ format_entry(mailinglist_id, title) }}
            {% endif %}
        {% endfor %}
    </ul>
    <h2>Versammlungslisten</h2>
    <ul>
        {% for mailinglist_id, title in mailinglists|dictsort %}
            {% if  all_mailinglists[mailinglist_id]['assembly_id'] %}
                {{ format_entry(mailinglist_id, title) }}
            {% endif %}
        {% endfor %}
    </ul>
{% endblock %}
