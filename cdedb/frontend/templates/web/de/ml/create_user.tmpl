{% extends "web/de/ml/base.tmpl" %}
{% import "web/de/util.tmpl" as util with context %}
{% block title %}Neuen Mailinglistennutzer anlegen{% endblock %}
{% block content %}
    <form action="{{ cdedblink('ml/create_user')|e }}" method="POST" id="newuserform">
        <table>
            {{ util.tab_input_text(name="given_names", label="Vorname(n)") }}
            {{ util.tab_input_text(name="family_name", label="Nachname") }}
            {{ util.tab_input_text(name="display_name", label="Rufname") }}
            {{ util.tab_input_text(name="username", label="Email") }}
            {{ util.tab_input_textarea(name="notes", label="Notizen", aclass="biggg") }}
        </table>
        <div>
            {{ util.input_submit(value="Anlegen") }}
            <a href="{{ cdedblink('ml/index')|e }}">Abbrechen</a>
        </div>
    </form>
{% endblock %}
