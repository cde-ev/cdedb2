{% set sidenav_active='assembly_config' %}
{% extends "web/de/assembly/base.tmpl" %}
{% import "web/de/util.tmpl" as util with context %}
{% block title %}
    {% if ambience['ballot'] %}
        Datei anhängen ({{ ambience['assembly']['title']|e }}/{{ ambience['ballot']['title']|e }})
    {% else %}
        Datei anhängen ({{ ambience['assembly']['title']|e }})
    {% endif %}
{% endblock %}
{% block content %}
    Bitte beachte, dass nur PDF-Dokumente zulässig sind.

    <form action="{{ cdedblink('assembly/add_attachment')|e }}" method="POST" enctype="multipart/form-data"
     id="addattachmentform">
        <div>
            {{ util.form_input_text(name="title", label="Bezeichnung") }}
            {{ util.form_input_text(name="filename", label="Dateiname",
                                    info="Leer lassen für derzeitigen Dateinamen.") }}
            {{ util.form_input_file(name="attachment", accept="application/pdf") }}
            {{ util.form_input_submit(value="Hochladen") }}
        </div>
    </form>
{% endblock %}
