#!/usr/bin/env python3

"""Routing table for the WSGI-application. This will get pretty big so put
it here where it is out of the way.
"""

import werkzeug.routing


class FilenameConverter(werkzeug.routing.BaseConverter):
    """Handles filename inputs in URL path."""

    regex = "[a-zA-Z0-9][-a-zA-Z0-9._]*"


def get_(
    string: str, endpoint: str, redirect_to: str | None = None
) -> werkzeug.routing.Rule:
    """Light wrapper to avoid having to specify the methods everytime.

    The _ in the name is so that the length aligns with `post` below.
    """
    return werkzeug.routing.Rule(
        string=string,
        endpoint=endpoint,
        redirect_to=redirect_to,
        methods=("GET", "HEAD"),
    )


def post(string: str, endpoint: str) -> werkzeug.routing.Rule:
    """Light wrapper to avoid having to specify the methods everytime."""
    return werkzeug.routing.Rule(string=string, endpoint=endpoint, methods=("POST",))


def sub(path: str, *rules: werkzeug.routing.RuleFactory) -> werkzeug.routing.Submount:
    """Light wrapper to remove one level of indentation for the list of rules."""
    return werkzeug.routing.Submount(path, rules)


def prefix(
    prefix_: str, *rules: werkzeug.routing.RuleFactory
) -> werkzeug.routing.EndpointPrefix:
    """Light wrapper to remove one level of indentation for the list of rules."""
    return werkzeug.routing.EndpointPrefix(prefix_, rules)


# shorthands, mainly to avoid missing the comma to make a tuple for POST
_GET = ("GET", "HEAD")
_POST = ("POST",)

#: Using a routing map allows to do lookups as well as the reverse process
#: of generating links to targets instead of hardcoding them.


rules = [
    prefix(
        "core/",
        get_("/", endpoint="index"),
        sub(
            "/core",
            get_("/api/resolve", endpoint="api_resolve_username"),
            post("/markdown/parse", endpoint="markdown_parse"),
            post("/login", endpoint="login"),
            post("/logout", endpoint="logout"),
            post("/logout/all", endpoint="logout_all"),
            post("/locale", endpoint="change_locale"),
            post("/admin_views", endpoint="modify_active_admin_views"),
            get_("/admins", endpoint="view_admins"),
            sub(
                "/contact",
                get_("/", endpoint="contact_form"),
                post("/", endpoint="contact"),
                get_("/reply", endpoint="contact_reply_form"),
                post("/reply", endpoint="contact_reply"),
                get_("/rotate", endpoint="rotate_anonymous_message"),
            ),
            get_("/log", endpoint="view_log"),
            get_("/meta", endpoint="meta_info_form"),
            post("/meta", endpoint="change_meta_info"),
            get_("/user/create", endpoint="create_user_form"),
            get_("/user/create/redirect", endpoint="create_user"),
            get_("/persona/adminshow", endpoint="admin_show_user"),
            get_("/persona/select", endpoint="select_persona"),
            get_("/changelog/list", endpoint="list_pending_changes"),
            get_("/changelog/view", endpoint="view_changelog_meta"),
            get_("/foto/<filename:foto>", endpoint="get_foto"),
            get_("/debugemail/<token>", endpoint="debug_email"),
            get_("/query/share", endpoint="query_by_name"),
            sub(
                "/self",
                get_("/show", endpoint="mydata"),
                get_("/change", endpoint="change_user_form"),
                post("/change", endpoint="change_user"),
                get_("/password/change", endpoint="change_password_form"),
                post("/password/change", endpoint="change_password"),
                get_("/events", endpoint="show_user_events_self"),
                get_("/mailinglists", endpoint="show_user_mailinglists_self"),
                get_("/lastschrift", endpoint="my_lastschrift"),
            ),
            sub(
                "/self/username",
                get_("/change", endpoint="change_username_form"),
                get_("/change/mail", endpoint="send_username_change_link"),
                get_("/change/confirm", endpoint="do_username_change_form"),
                post("/change", endpoint="do_username_change"),
            ),
            sub(
                "/password/reset",
                get_("/", endpoint="reset_password_form"),
                post("/", endpoint="send_password_reset_link"),
                get_("/confirm", endpoint="do_password_reset_form"),
                post("/confirm", endpoint="do_password_reset"),
            ),
            get_("/search/user", endpoint="user_search"),
            sub(
                "/emailstatus",
                get_("/list", endpoint="email_status_overview"),
                post("/set", endpoint="set_email_status"),
                post("/delete", endpoint="delete_email_status"),
            ),
            sub(
                "/persona/<int:persona_id>",
                get_("/adminchange", endpoint="admin_change_user_form"),
                post("/adminchange", endpoint="admin_change_user"),
                post("/password/reset", endpoint="admin_send_password_reset_link"),
                post("/password/invalidate", endpoint="invalidate_password"),
                get_("/privileges", endpoint="change_privileges_form"),
                post("/privileges", endpoint="change_privileges"),
                get_("/promote", endpoint="promote_user_form"),
                post("/promote", endpoint="promote_user"),
                get_("/membership/change", endpoint="modify_membership_form"),
                post("/membership/change", endpoint="modify_membership"),
                get_("/balance/change", endpoint="modify_balance_form"),
                post("/balance/change", endpoint="modify_balance"),
                get_("/foto/change", endpoint="set_foto_form"),
                post("/foto/change", endpoint="set_foto"),
                get_("/username/adminchange", endpoint="admin_username_change_form"),
                post("/username/adminchange", endpoint="admin_username_change"),
                get_("/changelog/inspect", endpoint="inspect_change"),
                post("/changelog/resolve", endpoint="resolve_change"),
                get_("/show", endpoint="show_user"),
                get_("/events", endpoint="show_user_events"),
                get_("/mailinglists", endpoint="show_user_mailinglists"),
                get_("/measures", endpoint="show_user_measures"),
                get_("/vcard", endpoint="download_vcard"),
                get_("/vcard/qr", endpoint="qr_vcard"),
                get_("/history", endpoint="show_history"),
                post("/archive", endpoint="archive_persona"),
                get_("/dearchive", endpoint="dearchive_persona_form"),
                post("/dearchive", endpoint="dearchive_persona"),
                post("/purge", endpoint="purge_persona"),
                post("/activity/change", endpoint="toggle_activity"),
            ),
            sub(
                "/genesis",
                get_("/request", endpoint="genesis_request_form"),
                post("/request", endpoint="genesis_request"),
                get_("/verify", endpoint="genesis_verify"),
                get_("/list", endpoint="genesis_list_cases"),
                get_(
                    "/attachment/<filename:attachment_hash>",
                    endpoint="genesis_get_attachment",
                ),
            ),
            sub(
                "/genesis/<int:genesis_case_id>",
                get_("/show", endpoint="genesis_show_case"),
                get_("/modify", endpoint="genesis_modify_form"),
                post("/modify", endpoint="genesis_modify"),
                post("/modify/realm", endpoint="genesis_modify_realm"),
                post("/decide", endpoint="genesis_decide"),
            ),
            sub(
                "/privileges",
                get_("/list", endpoint="list_privilege_changes"),
            ),
            sub(
                "/privileges/<int:change_id>",
                get_("/show", endpoint="show_privilege_change"),
                post("/decide", endpoint="decide_privilege_change"),
            ),
            sub(
                "/complaint",
                get_("/index", endpoint="complaint_index"),
                get_("/log", endpoint="view_complaint_log"),
                get_("/measures", endpoint="measures"),
                get_("/helpers", endpoint="list_complaint_helpers"),
                post("/helpers/enforcer/add", endpoint="add_enforcer"),
                post("/helpers/enforcer/remove", endpoint="remove_enforcer"),
                get_("/case/create", endpoint="create_case_form"),
                post("/case/create", endpoint="create_case"),
                sub(
                    "/case/<int:case_id>",
                    get_("/show", endpoint="show_case"),
                    get_("/history", endpoint="case_history"),
                    get_("/export", endpoint="export_case"),
                    get_("/change", endpoint="change_case_form"),
                    post("/change", endpoint="change_case"),
                    get_(
                        "/attachment/<filename:attachment_hash>",
                        endpoint="get_cached_complaint_attachment",
                    ),
                    post("/involved/add", endpoint="add_involved"),
                    sub(
                        "/involved/<int:persona_id>",
                        get_("/companions/change", endpoint="manage_companions_form"),
                        post("/companions/add", endpoint="add_companions"),
                        sub(
                            "/companions/<int:companion_id>",
                            post("/remove", endpoint="remove_companion"),
                            post("/withdraw", endpoint="withdraw_companion"),
                            post("/reinstate", endpoint="reinstate_companion"),
                        ),
                        post("/inform", endpoint="inform_involved"),
                        post("/uninform", endpoint="uninform_involved"),
                        post("/remove", endpoint="remove_involved"),
                    ),
                    post("/unlock", endpoint="unlock_case"),
                    post("/lock", endpoint="lock_case"),
                    get_("/entry/add", endpoint="add_entry_form"),
                    post("/entry/add", endpoint="add_entry"),
                    sub(
                        "/entry/<int:entry_id>",
                        get_("/replace", endpoint="replace_entry_form"),
                        post("/replace", endpoint="replace_entry"),
                        get_("/revoke", endpoint="revoke_entry_form"),
                        post("/revoke", endpoint="revoke_entry"),
                        get_("/remove", endpoint="remove_entry_form"),
                        post("/remove", endpoint="remove_entry"),
                        sub(
                            "/version/<int:version_idx>",
                            get_("/attachment", endpoint="get_complaint_attachment"),
                        ),
                        sub(
                            "/version/<int:entry_version_id>",
                            post("/purge", endpoint="mark_entry_version_for_purge"),
                            post(
                                "/purge/cancel",
                                endpoint="unmark_entry_version_for_purge",
                            ),
                        ),
                    ),
                    sub(
                        "/entry/<int:parent_id>",
                        get_("/child/add", endpoint="add_entry_form"),
                        post("/child/add", endpoint="add_entry"),
                    ),
                ),
            ),
        ),
    ),
    prefix(
        "cde/",
        sub(
            "/cde",
            get_("/", endpoint="index"),
            get_("/qr", endpoint="membership_qr"),
            get_("/stats", endpoint="member_stats"),
            get_("/log", endpoint="view_cde_log"),
            get_("/misc", endpoint="view_misc"),
            get_("/finances", endpoint="view_finance_log"),
            get_("/user/create", endpoint="create_user_form"),
            post("/user/create", endpoint="create_user"),
            get_("/admission", endpoint="batch_admission_form"),
            post("/admission", endpoint="batch_admission"),
            get_("/parse", endpoint="parse_statement_form"),
            post("/parse", endpoint="parse_statement"),
            post("/parse/download", endpoint="parse_download"),
            get_("/transfers", endpoint="money_transfers_form"),
            post("/transfers", endpoint="money_transfers"),
            sub(
                "/self",
                get_("/consent", endpoint="consent_decision_form"),
                post("/consent", endpoint="consent_decision"),
            ),
            sub(
                "/search",
                get_("/member", endpoint="member_search"),
                get_("/user", endpoint="user_search"),
                get_("/course", endpoint="past_course_search"),
            ),
            get_("/i25p", endpoint="", redirect_to="cde/lastschrift/info"),
            sub(
                "/lastschrift/",
                get_("/", endpoint="lastschrift_index"),
                get_("/create", endpoint="lastschrift_create_form"),
                post("/create", endpoint="lastschrift_create"),
                get_("/form/download", endpoint="lastschrift_subscription_form"),
                get_("/form/fill", endpoint="lastschrift_subscription_form_fill"),
                get_("/info", endpoint="i25p_index"),
                get_("/transaction/download", endpoint="lastschrift_download_sepapain"),
                post(
                    "/transaction/generate",
                    endpoint="lastschrift_generate_transactions",
                ),
                post(
                    "/transaction/finalize",
                    endpoint="lastschrift_finalize_transactions",
                ),
            ),
            sub(
                "/past/",
                get_("/event/list", endpoint="list_past_events"),
                get_("/event/create", endpoint="create_past_event_form"),
                post("/event/create", endpoint="create_past_event"),
                get_("/log", endpoint="view_past_log"),
                sub(
                    "/event/<int:pevent_id>",
                    get_("/show", endpoint="show_past_event"),
                    get_("/download", endpoint="download_past_event_participantlist"),
                    get_("/change", endpoint="change_past_event_form"),
                    post("/change", endpoint="change_past_event"),
                    post("/delete", endpoint="delete_past_event"),
                    get_("/course/create", endpoint="create_past_course_form"),
                    post("/course/create", endpoint="create_past_course"),
                    post("/participant/set", endpoint="set_participants"),
                    post("/participant/remove", endpoint="remove_participant"),
                    sub(
                        "/course/<int:pcourse_id>",
                        post("/assignment/set", endpoint="set_course_assignments"),
                        post("/assignment/remove", endpoint="remove_course_assignment"),
                        get_("/show", endpoint="show_past_course"),
                        get_("/change", endpoint="change_past_course_form"),
                        post("/change", endpoint="change_past_course"),
                        post("/delete", endpoint="delete_past_course"),
                    ),
                ),
            ),
            sub(
                "/lastschrift/<int:lastschrift_id>",
                post("/skip", endpoint="lastschrift_skip"),
                get_("/change", endpoint="lastschrift_change_form"),
                post("/change", endpoint="lastschrift_change"),
                post("/revoke", endpoint="lastschrift_revoke"),
                sub(
                    "/transaction/<int:transaction_id>",
                    post("/finalize", endpoint="lastschrift_finalize_transaction"),
                    post("/rollback", endpoint="lastschrift_rollback_transaction"),
                ),
            ),
            sub(
                "/user/<int:persona_id>",
                get_("/lastschrift", endpoint="lastschrift_show"),
                get_("/lastschrift/create", endpoint="lastschrift_create_form"),
                post("/lastschrift/create", endpoint="lastschrift_create"),
            ),
            sub(
                "/semester",
                get_("/show", endpoint="show_semester"),
                post("/bill", endpoint="semester_bill"),
                post("/eject", endpoint="semester_eject"),
                post("/balance", endpoint="semester_balance_update"),
            ),
            sub(
                "/expuls",
                post("/address", endpoint="expuls_addresscheck"),
                post("/advance", endpoint="expuls_advance"),
            ),
        ),
    ),
    prefix(
        "event/",
        sub(
            "/event",
            get_("/", endpoint="index"),
            get_("/search/user", endpoint="user_search"),
            get_("/registration/select", endpoint="select_registration"),
            get_("/offline/partial", endpoint="download_quick_partial_export"),
            get_("/user/create", endpoint="create_user_form"),
            post("/user/create", endpoint="create_user"),
            get_("/log", endpoint="view_log"),
            get_("/helpers/list", endpoint="list_event_helpers"),
            post("/helpers/add", endpoint="add_event_helper"),
            post("/helpers/remove", endpoint="remove_event_helper"),
            sub(
                "/event",
                get_("/list", endpoint="list_events"),
                get_("/create", endpoint="create_event_form"),
                post("/create", endpoint="create_event"),
                get_("/redirect", endpoint="redirect_event"),
                get_("/violations", endpoint="constraint_violations_summary"),
                sub(
                    "/droid",
                    get_("/export", endpoint="droid_partial_export_dispatch"),
                ),
            ),
            sub(
                "/event/<int:event_id>",
                get_("/show", endpoint="show_event"),
                get_("/log", endpoint="view_event_log"),
                post("/ml/create", endpoint="create_event_mailinglist"),
                get_("/stats", endpoint="stats"),
                get_("/notes", endpoint="participant_info"),
                get_("/export", endpoint="download_export"),
                post("/lock", endpoint="lock_event"),
                post("/unlock", endpoint="unlock_event"),
                post("/archive", endpoint="archive_event"),
                post("/delete", endpoint="delete_event"),
                post("/balance", endpoint="balance_event"),
                post("/unbalance", endpoint="unbalance_event"),
                sub(
                    "/payment",
                    get_("/", endpoint="event_payment"),
                    get_("/qr", endpoint="event_payment_qrcode"),
                ),
                get_("/change", endpoint="change_event_form"),
                post("/change", endpoint="change_event"),
                get_("/freetexts", endpoint="show_free_texts"),
                post("/freetexts", endpoint="change_free_text"),
                get_("/batchfees", endpoint="batch_fees_form"),
                post("/batchfees", endpoint="batch_fees"),
                sub(
                    "/droid",
                    # API-functionality:
                    get_("/partial", endpoint="droid_partial_export"),
                    # Token management:
                    get_("/summary", endpoint="orga_token_summary"),
                    get_("/create", endpoint="create_orga_token_form"),
                    post("/create", endpoint="create_orga_token"),
                    sub(
                        "/<int:orga_token_id>",
                        get_("/change", endpoint="change_orga_token_form"),
                        post("/change", endpoint="change_orga_token"),
                        post("/delete", endpoint="delete_orga_token"),
                        post("/revoke", endpoint="revoke_orga_token"),
                    ),
                ),
                sub(
                    "/minorform",
                    get_("/get", endpoint="get_minor_form"),
                    post("/change", endpoint="change_minor_form"),
                ),
                get_("/roles/manage", endpoint="manage_roles"),
                sub(
                    "/orga",
                    post("/add", endpoint="add_orgas"),
                    post("/remove", endpoint="remove_orga"),
                ),
                sub(
                    "/caretaker",
                    post("/add", endpoint="add_caretakers"),
                    post("/remove", endpoint="remove_caretaker"),
                ),
                sub(
                    "/checkin_helper",
                    post("/add", endpoint="add_checkin_helpers"),
                    post("/remove", endpoint="remove_checkin_helper"),
                ),
                sub(
                    "/query",
                    sub(
                        "/filter",
                        get_("/", endpoint="custom_filter_summary"),
                        sub(
                            "/create",
                            post("/", endpoint="create_custom_filter"),
                            get_(
                                "/registration", endpoint="create_registration_filter"
                            ),
                            get_("/course", endpoint="create_course_filter"),
                            get_("/lodgement", endpoint="create_lodgement_filter"),
                        ),
                        sub(
                            "/<int:custom_filter_id>",
                            get_("/change", endpoint="change_custom_filter_form"),
                            post("/change", endpoint="change_custom_filter"),
                            post("/delete", endpoint="delete_custom_filter"),
                        ),
                    ),
                    post("/store", endpoint="store_event_query"),
                    post("/delete", endpoint="delete_event_query"),
                    get_("/share", endpoint="event_query_by_name"),
                ),
                get_("/register", endpoint="register_form"),
                post("/register", endpoint="register"),
                sub(
                    "/register",
                    get_("/config", endpoint="configure_registration_form"),
                    post("/config", endpoint="configure_registration"),
                    get_("/fee", endpoint="precompute_fee"),
                ),
                sub(
                    "/questionnaire",
                    get_("/config", endpoint="configure_additional_questionnaire_form"),
                    post("/config", endpoint="configure_additional_questionnaire"),
                    get_("/reorder", endpoint="reorder_questionnaire_form"),
                    post("/reorder", endpoint="reorder_questionnaire"),
                ),
                sub(
                    "/import",
                    get_("/partial", endpoint="partial_import_form"),
                    post("/partial", endpoint="partial_import"),
                    get_("/questionnaire", endpoint="questionnaire_import_form"),
                    post("/questionnaire", endpoint="questionnaire_import"),
                ),
                get_("/download", endpoint="downloads"),
                sub(
                    "/download",
                    get_("/nametag", endpoint="download_nametags"),
                    get_("/coursepuzzle", endpoint="download_course_puzzle"),
                    get_("/lodgementpuzzle", endpoint="download_lodgement_puzzle"),
                    get_("/courselists", endpoint="download_course_lists"),
                    get_("/lodgementlists", endpoint="download_lodgement_lists"),
                    get_("/participantlist", endpoint="download_participant_list"),
                    get_("/dokuteam_course", endpoint="download_dokuteam_courselist"),
                    get_(
                        "/dokuteam_participant",
                        endpoint="download_dokuteam_participant_list",
                    ),
                    get_("/partial", endpoint="download_partial_export"),
                    get_("/questionnaire", endpoint="download_questionnaire_export"),
                    get_("/csv_courses", endpoint="download_csv_courses"),
                    get_("/csv_lodgements", endpoint="download_csv_lodgements"),
                    get_("/csv_registrations", endpoint="download_csv_registrations"),
                ),
                sub(
                    "/group",
                    get_("/summary", endpoint="group_summary"),
                    get_("/violations", endpoint="constraint_violations"),
                ),
                sub(
                    "/fee",
                    get_("/summary", endpoint="fee_summary"),
                    get_("/stats", endpoint="fee_stats"),
                    get_("/add", endpoint="configure_fee_form"),
                    post("/add", endpoint="configure_fee"),
                    get_("/set", endpoint="personalized_fee_multiset_form"),
                    sub(
                        "/<int:fee_id>",
                        get_("/change", endpoint="configure_fee_form"),
                        post("/change", endpoint="configure_fee"),
                        post("/delete", endpoint="delete_fee"),
                        get_("/set", endpoint="personalized_fee_multiset_form"),
                        post("/set", endpoint="personalized_fee_multiset"),
                    ),
                ),
                sub(
                    "/part",
                    get_("/summary", endpoint="part_summary"),
                    get_("/add", endpoint="add_part_form"),
                    post("/add", endpoint="add_part"),
                    sub(
                        "/<int:part_id>",
                        get_("/change", endpoint="change_part_form"),
                        post("/change", endpoint="change_part"),
                        post("/delete", endpoint="delete_part"),
                    ),
                    sub(
                        "/group",
                        get_("/add", endpoint="add_part_group_form"),
                        post("/add", endpoint="add_part_group"),
                        sub(
                            "/<int:part_group_id>",
                            get_("/change", endpoint="change_part_group_form"),
                            post("/change", endpoint="change_part_group"),
                            post("/delete", endpoint="delete_part_group"),
                        ),
                    ),
                ),
                sub(
                    "/track/group",
                    get_("/add", endpoint="add_track_group_form"),
                    post("/add", endpoint="add_track_group"),
                    sub(
                        "/<int:track_group_id>",
                        get_("/change", endpoint="change_track_group_form"),
                        post("/change", endpoint="change_track_group"),
                        post("/delete", endpoint="delete_track_group"),
                    ),
                ),
                sub(
                    "/course",
                    get_("/list", endpoint="course_list"),
                    get_("/stats", endpoint="course_stats"),
                    get_("/create", endpoint="create_course_form"),
                    post("/create", endpoint="create_course"),
                    get_("/query", endpoint="course_query"),
                    get_("/choices", endpoint="course_choices_form"),
                    post("/choices", endpoint="course_choices"),
                    get_("/checks", endpoint="course_assignment_checks"),
                    get_("/instructed", endpoint="show_instructed_courses"),
                    sub(
                        "/<int:course_id>",
                        get_("/show", endpoint="show_course"),
                        get_("/change", endpoint="change_course_form"),
                        post("/change", endpoint="change_course"),
                        post("/delete", endpoint="delete_course"),
                        get_("/manage", endpoint="manage_attendees_form"),
                        post("/manage", endpoint="manage_attendees"),
                    ),
                ),
                sub(
                    "/registration",
                    get_("/add", endpoint="add_registration_form"),
                    post("/add", endpoint="add_registration"),
                    get_("/query", endpoint="registration_query"),
                    get_("/status", endpoint="registration_status"),
                    get_("/amend", endpoint="amend_registration_form"),
                    post("/amend", endpoint="amend_registration"),
                    get_("/questionnaire", endpoint="additional_questionnaire_form"),
                    post("/questionnaire", endpoint="additional_questionnaire"),
                    get_("/quick", endpoint="quick_show_registration"),
                    get_("/list", endpoint="participant_list"),
                    get_("/multiedit", endpoint="change_registrations_form"),
                    post("/multiedit", endpoint="change_registrations"),
                    post("/approve", endpoint="approve_registration"),
                    post("/unapprove", endpoint="unapprove_registration"),
                    sub(
                        "/checkin",
                        get_("/", endpoint="checkin_form"),
                        get_("/multiset", endpoint="checkin_multiset_form"),
                        post("/multiset", endpoint="checkin_multiset"),
                    ),
                    sub(
                        "/<int:registration_id>",
                        get_("/show", endpoint="show_registration"),
                        sub(
                            "/checkin",
                            post("/checkin", endpoint="add_checkin"),
                            post("/checkout", endpoint="add_checkout"),
                            post("/add", endpoint="add_backdated_checkin_period"),
                            post("/change", endpoint="change_checkin_period"),
                            post("/delete", endpoint="delete_checkin_period"),
                        ),
                        sub(
                            "/fee",
                            get_("/summary", endpoint="show_registration_fee"),
                            get_("/qr", endpoint="registration_fee_qr"),
                            get_("/add", endpoint="add_new_personalized_fee_form"),
                            post("/add", endpoint="add_new_personalized_fee"),
                            sub(
                                "/<int:fee_id>",
                                post("/add", endpoint="add_personalized_fee"),
                                post("/delete", endpoint="delete_personalized_fee"),
                            ),
                        ),
                        get_("/change", endpoint="change_registration_form"),
                        post("/change", endpoint="change_registration"),
                        post("/delete", endpoint="delete_registration"),
                    ),
                ),
                sub(
                    "/lodgement",
                    get_("/overview", endpoint="lodgements"),
                    get_("/create", endpoint="create_lodgement_form"),
                    post("/create", endpoint="create_lodgement"),
                    get_("/query", endpoint="lodgement_query"),
                    sub(
                        "/group",
                        get_("/summary", endpoint="lodgement_group_summary_form"),
                        post("/summary", endpoint="lodgement_group_summary"),
                        sub(
                            "/<int:group_id>",
                            get_("/move", endpoint="move_lodgements_form"),
                            post("/move", endpoint="move_lodgements"),
                        ),
                    ),
                    sub(
                        "/graph",
                        get_("/show", endpoint="lodgement_wishes_graph"),
                        get_("/form", endpoint="lodgement_wishes_graph_form"),
                    ),
                    sub(
                        "/<int:lodgement_id>",
                        get_("/change", endpoint="change_lodgement_form"),
                        post("/change", endpoint="change_lodgement"),
                        post("/delete", endpoint="delete_lodgement"),
                        get_("/manage", endpoint="manage_inhabitants_form"),
                        post("/manage", endpoint="manage_inhabitants"),
                        post("/swap", endpoint="swap_inhabitants"),
                        get_("/show", endpoint="show_lodgement"),
                    ),
                ),
                sub(
                    "/field",
                    get_("/setselect", endpoint="field_multiset_select"),
                    get_("/summary", endpoint="field_summary_form"),
                    post("/summary", endpoint="field_summary"),
                    sub(
                        "/<int:field_id>",
                        get_("/set", endpoint="field_multiset_form"),
                        post("/set", endpoint="field_multiset"),
                    ),
                ),
            ),
        ),
    ),
    prefix(
        "assembly/",
        sub(
            "/assembly",
            get_("/", endpoint="index"),
            get_("/verify_result.pyz", endpoint="download_verify_result_script"),
            get_("/search/user", endpoint="user_search"),
            get_("/user/create", endpoint="create_user_form"),
            post("/user/create", endpoint="create_user"),
            get_("/assembly/create", endpoint="create_assembly_form"),
            post("/assembly/create", endpoint="create_assembly"),
            get_("/log", endpoint="view_log"),
            sub(
                "/assembly/<int:assembly_id>",
                get_("/show", endpoint="show_assembly"),
                post("/presider/add", endpoint="add_presiders"),
                post("/presider/remove", endpoint="remove_presider"),
                post("/delete", endpoint="delete_assembly"),
                get_("/log", endpoint="view_assembly_log"),
                get_("/change", endpoint="change_assembly_form"),
                post("/change", endpoint="change_assembly"),
                post("/ml/create", endpoint="create_assembly_mailinglist"),
                post("/signup", endpoint="signup"),
                post("/signup/external", endpoint="external_signup"),
                get_("/attachments", endpoint="list_attachments"),
                get_("/attendees", endpoint="list_attendees"),
                get_("/attendees/download", endpoint="download_list_attendees"),
                post("/conclude", endpoint="conclude_assembly"),
                sub(
                    "/attachment",
                    get_("/add", endpoint="add_attachment_form"),
                    post("/add", endpoint="add_attachment"),
                    get_(
                        "/cached/<filename:attachment_hash>",
                        endpoint="get_cached_attachment",
                    ),
                    sub(
                        "/<int:attachment_id>",
                        get_("/latest", endpoint="get_attachment"),
                        # allow this simple link for convenience
                        get_(
                            "/",
                            endpoint="",
                            redirect_to="assembly/assembly/<assembly_id>/attachment/<attachment_id>/latest",
                        ),
                        # this is kept as legacy support
                        get_(
                            "/get",
                            endpoint="",
                            redirect_to="assembly/assembly/<assembly_id>/attachment/<attachment_id>/latest",
                        ),
                        post("/delete", endpoint="delete_attachment"),
                        get_("/add", endpoint="add_attachment_version_form"),
                        post("/add", endpoint="add_attachment_version"),
                        sub(
                            "/version/<int:version_nr>",
                            get_("/", endpoint="get_attachment_version"),
                            # this is kept as legacy support
                            get_(
                                "/get",
                                endpoint="",
                                redirect_to="assembly/assembly/<assembly_id>/attachment/<attachment_id>/version/<version_nr>",
                            ),
                            get_("/change", endpoint="change_attachment_version_form"),
                            post("/change", endpoint="change_attachment_version"),
                            post("/delete", endpoint="delete_attachment_version"),
                        ),
                    ),
                ),
                get_("/ballot/list", endpoint="list_ballots"),
                get_("/ballot/create", endpoint="create_ballot_form"),
                post("/ballot/create", endpoint="create_ballot"),
                get_("/ballot/summary", endpoint="summary_ballots"),
                get_("/ballot/reschedule", endpoint="reschedule_ballots_form"),
                post("/ballot/reschedule", endpoint="reschedule_ballots"),
                sub(
                    "/ballot/<int:ballot_id>",
                    get_("/show", endpoint="show_ballot"),
                    get_("/change", endpoint="change_ballot_form"),
                    post("/change", endpoint="change_ballot"),
                    get_("/comment", endpoint="comment_concluded_ballot_form"),
                    post("/comment", endpoint="comment_concluded_ballot"),
                    post("/delete", endpoint="delete_ballot"),
                    post("/start", endpoint="ballot_start_voting"),
                    post("/vote", endpoint="vote"),
                    get_("/result", endpoint="show_ballot_result"),
                    post("/result", endpoint="show_old_vote"),
                    get_("/result/download", endpoint="get_result"),
                    sub(
                        "/attachment",
                        sub(
                            "/<int:attachment_id>",
                            # this is kept as legacy support
                            get_(
                                "/get",
                                endpoint="",
                                redirect_to="assembly/assembly/<assembly_id>/attachment/<attachment_id>/latest",
                            ),
                            sub(
                                "/version/<int:version>",
                                # this is kept as legacy support
                                get_(
                                    "/get",
                                    endpoint="",
                                    redirect_to="assembly/assembly/<assembly_id>/attachment/<attachment_id>/version/<version>",
                                ),
                            ),
                        ),
                    ),
                    post("/candidates/edit", endpoint="edit_candidates"),
                    get_("/template", endpoint="ballot_template"),
                    get_("/template/redirect", endpoint="ballot_template_redirect"),
                ),
            ),
        ),
    ),
    prefix(
        "ml/",
        sub(
            "/ml",
            get_("/", endpoint="index"),
            get_("/search/user", endpoint="user_search"),
            sub(
                "/user",
                get_("/create", endpoint="create_user_form"),
                post("/create", endpoint="create_user"),
                get_("/merge", endpoint="merge_accounts_form"),
                post("/merge", endpoint="merge_accounts"),
            ),
            post("/mailinglist/refresh", endpoint="manually_write_subscription_states"),
            post("/mailinglist/sync", endpoint="manual_mailman_sync"),
            get_("/mailinglist/list", endpoint="list_mailinglists"),
            get_("/mailinglist/moderated", endpoint="moderated_mailinglists"),
            get_("/mailinglist/create", endpoint="create_mailinglist_form"),
            post("/mailinglist/create", endpoint="create_mailinglist"),
            get_("/log", endpoint="view_log"),
            sub(
                "/mailinglist/<int:mailinglist_id>",
                get_("/show", endpoint="show_mailinglist"),
                get_("/change", endpoint="change_mailinglist_form"),
                post("/change", endpoint="change_mailinglist"),
                get_("/type/change", endpoint="change_ml_type_form"),
                post("/type/change", endpoint="change_ml_type"),
                post("/delete", endpoint="delete_mailinglist"),
                get_("/log", endpoint="view_ml_log"),
                get_("/management", endpoint="management"),
                get_("/moderate", endpoint="message_moderation_form"),
                post("/moderate", endpoint="message_moderation"),
                post("/moderate/multi", endpoint="message_moderation_multi"),
                get_("/management/advanced", endpoint="advanced_management"),
                get_("/roster", endpoint="show_roster"),
                get_(
                    "/download/subscriptions",
                    endpoint="download_csv_subscription_states",
                ),
                post("/force/add", endpoint="add_subscription_overrides"),
                post("/force/remove", endpoint="remove_subscription_override"),
                post("/block/add", endpoint="add_unsubscription_overrides"),
                post("/block/remove", endpoint="remove_unsubscription_override"),
                post("/moderator/add", endpoint="add_moderators"),
                post("/moderator/remove", endpoint="remove_moderator"),
                post("/whitelist/add", endpoint="add_whitelist"),
                post("/whitelist/remove", endpoint="remove_whitelist"),
                post("/requests/handle", endpoint="handle_request"),
                post("/subscriptions/add", endpoint="add_subscribers"),
                post("/subscriptions/remove", endpoint="remove_subscriber"),
                post("/subscriptions/readd", endpoint="readd_subscriber"),
                post("/subscriptions/reset", endpoint="reset_unsubscription"),
                post("/subscriptions/self/add", endpoint="subscribe"),
                post("/subscriptions/self/remove", endpoint="unsubscribe"),
                post("/subscriptions/self/request", endpoint="request_subscription"),
                post("/subscriptions/self/cancel", endpoint="cancel_subscription"),
                post("/subaddress/change", endpoint="change_address"),
                get_("/subaddress/confirm", endpoint="do_address_change"),
            ),
        ),
    ),
]

CDEDB_PATHS = werkzeug.routing.Map(
    rules=rules,
    converters={"filename": FilenameConverter},
)
