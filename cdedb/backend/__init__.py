#!/usr/bin/env python3

"""The backend consists of several parts which are accessible via RPC. Each
part handles one realm (with the exception of an additional session
backend).

Each backend service module when executed as a python script runs a
backend server. This requires a running :py:mod:`Pyro4` nameserver.
"""
pass
