#!/usr/bin/env python3

"""Global configuration for test runs.

Has to be included via ``cdedb.localconfig``.

Normally all configuration for test runs should be done via
``test.localconfig``, but in that way it's not possible to override
settings in the BasicConfig which is done via this file.
"""

import pathlib

GLOBAL_LOG = pathlib.Path("/tmp/test-cdedb.log")
CONSOLE_LOG_LEVEL = None
DB_PORT = 6432
