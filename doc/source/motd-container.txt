# reset running application (update with current code / settings)
cd /cdedb2 && make reload

# run (part of the) test suite (all tests matching the given pattern)
# filename may be included in pattern; when no pattern given run everything
/cdedb2/bin/check.py test_backend_event*test_json_fields_with_dates

# access postgres database directly (from host!)
docker compose exec cdb psql -U cdb -d cdb

# list CdEDB log files (most recently changed first)
ls -lht /var/log/cdedb

# reset database (DESTROYS ALL DATA)
cd /cdedb2 && make sample-data
